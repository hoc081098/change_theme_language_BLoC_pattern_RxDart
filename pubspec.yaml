name: change_theme_language_bloc
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.2
  flutter_bloc_pattern: ^0.0.1+1
  distinct_value_connectable_observable: ^1.0.0+1
  shared_preferences: ^0.4.3
  dio: ^2.0.0
  collection: ^1.14.11
  url_launcher: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/en.png
    - assets/images/vi.png
